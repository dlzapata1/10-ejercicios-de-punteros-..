#include <iostream>
using namespace std;

int main() {
    int a, b;
    int *p1 = &a;
    int *p2 = &b;

    cout << "Ingrese dos numeros: ";
    cin >> *p1 >> *p2;

    cout << "Suma = " << *p1 + *p2 << endl;

    return 0;
}
